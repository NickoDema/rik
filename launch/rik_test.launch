<launch>

  <param name="testy_description"  command="$(find xacro)/xacro --inorder '$(find rik)/robots/TESTy/urdf/testy.xacro'" />
  <param name="testy2_description" command="$(find xacro)/xacro --inorder '$(find rik)/robots/TESTy/urdf/testy.xacro'" />

  <!-- <node name="rviz" pkg="rviz" type="rviz" args="-d $(find iiwa)/config/kuka_iiwa.rviz" required="true"/>-->

  <node pkg="rik" type="rik_test_node.py" name="rik" output="screen">
    <rosparam param="descriptions">
      [ { name: 'testy',  dtype: 'urdf', data: 'testy_description' },
        { name: 'testy2', dtype: 'urdf', data: 'testy2_description' } ]
    </rosparam>
    <!-- <param name="use_gui" value="true" /> -->
    <!-- <rosparam param="source_list">[arm_state]</rosparam> -->
  </node>

</launch>
